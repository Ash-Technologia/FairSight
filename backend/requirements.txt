# FairSight Backend Dependencies
# Using loose version constraints so pip can find pre-built wheels on any Python version.
# Pinning exact versions on compiled packages (pydantic-core, grpcio, etc.) causes
# Rust/Cargo build failures on Render when no wheel exists for that Python version.

# ── Core API ──────────────────────────────────────────────────────────────────
fastapi>=0.110,<0.120
uvicorn[standard]>=0.29,<0.35
starlette>=0.36,<0.40
python-multipart>=0.0.9
python-dotenv>=1.0

# ── Pydantic (let pip pick a version with a pre-built wheel) ──────────────────
pydantic>=2.5,<3

# ── Data Science ──────────────────────────────────────────────────────────────
pandas>=2.0,<3
numpy>=1.24,<2
scikit-learn>=1.3,<2
scipy>=1.10,<2
fairlearn>=0.9,<1

# ── Google / Firebase ──────────────────────────────────────────────────────────
firebase-admin>=6.0,<7
google-generativeai>=0.8,<1
google-cloud-firestore>=2.14,<3
google-auth>=2.20,<3

# ── HTTP / Networking ─────────────────────────────────────────────────────────
httpx>=0.25,<0.30
requests>=2.30,<3
websockets>=11,<14

# ── Utilities ─────────────────────────────────────────────────────────────────
PyJWT>=2.8,<3
PyYAML>=6.0
tqdm>=4.60
